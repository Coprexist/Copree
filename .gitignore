# ── 环境与密钥 ──
.env
.env.local
.env.*.local

# 敏感 token（JWT 等）
token.txt

# 备份文件
*.bak-*
backup_backend/

# ── Docker 数据卷（数据库、上传文件） ──
data/*
!data/world_blocks/

# ── Python ──
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg
.venv/
venv/

# ── 构建产物 ──
*.exe
!dist/Copree/Copree.exe
installer.nsi
LICENSE.txt

# ── Node ──
node_modules/
.pnp/
.pnp.js

# ── IDE / 编辑器 ──
.idea/
.vscode/
.trae/
*.swp
*.swo
*~

# ── 操作系统 ──
.DS_Store
Thumbs.db
Desktop.ini

# ── 日志 ──
*.log
*.log.*

# ── 其他 ──
*.tmp
*.bak

# ── Claude 内部文件（绝不公开） ──
CLAUDE.md
.claude/

# ── dsh 本地技能（仅本地会话使用，绝不公开） ──
.dsh/

# ── 冻结本文件（后续本地修改不提交） ──
.gitignore
.agent-memory.md
core.*
backend/core.*
frontend/public/docs/
Copree-Client

# dsh-copree 构建符号链接
/dsh-copree/node_modules
/study-room/

# Copree 世界本地镜像与附件（运行时产物，不进仓库）
/.copree/

# Python/venv
.venv*

# app data
data/
*.log
__pycache__/
frontend/dist/

# 测试产物（覆盖率报告 / pytest 缓存）
.coverage
.coverage.*
coverage.xml
htmlcov/
.pytest_cache/

# 本地私有资料：审计/渗透报告、口令与密钥备忘、内网地址清单、运维记录……
# 一律放 gitignore-本地docs/（目录名一目了然：这里的东西不入库），
# 约定见 docs/SUMMARY.md「文档规范 · 本地私有资料」
gitignore-本地docs/

# 兜底：审计类报告即使被随手放在别处，也不进仓库
SECURITY-AUDIT-*.md
PENTEST-*.md

